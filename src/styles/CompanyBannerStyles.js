import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  companyContainer: {
    marginBottom: '3rem',
  },
  companyHeading: {
    margin: '3rem 0 2rem',
    textAlign: 'center',
  },
  companyItem: {
    width: 200,
    marginRight: '0.5rem',
  },
  iconImage: {
    height: '100%',
    width: '100%',
  },
}));
