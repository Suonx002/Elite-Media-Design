import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  companyHeading: {
    margin: '2rem 0',
    textAlign: 'center',
  },
  companyItem: {
    width: 200,
    marginRight: '0.5rem',
  },
  iconImage: {
    height: '100%',
    width: '100%',
  },
}));
